package com.codeshift.sandbox;

/** A generated Java source unit to be compiled. */
public record JavaSourceFile(String className, String source) {
    // className is the fully-qualified name, e.g. com.acme.web.OrderController
}
